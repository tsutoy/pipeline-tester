name: Frontend CI

on:
  workflow_call:
    outputs:
      version:
        description: "New frontend version"
        value: ${{ jobs.bump-and-package.outputs.version }}
      bump_commit_id:
        description: "Auto commit if of bump version"
        value: ${{ jobs.bump-and-package.outputs.bump_commit_id }}
      artifact_name:
        description: "App package artifact name"
        value: ${{ jobs.bump-and-package.outputs.artifact_name }}


env:
  WORKING_DIRECTORY: apps/sidebar

jobs:
  check-next-bump-version:
    uses: ./.github/workflows/bump-version.yaml
    with:
      context_name: frontend
      working_directory: ${{ env.WORKING_DIRECTORY }}
      auto_commit: false

  package:
    needs: [check-next-bump-version]
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Update repository
        run: git pull

      - name: Setup Node
        uses: actions/setup-node@v3
        id: setup-node
        with:
          node-version-file: '.node-version'

      - name: Setup Cache Node_Modules
        id: cache-node-modules
        uses: actions/cache@v3
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-${{ steps.setup-node.outputs['node-version'] }}-node_modules-${{ hashFiles('**/yarn.lock') }}

      - name: Setup Dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: yarn install

      - name: Bump manifest version
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          sed -i s/{{version}}/${{ steps.bump.outputs.version }}/g ./manifest.json

      - name: Package app
        run: zip -r "app_${{ steps.bump.outputs.version }}.zip" manifest.json
        working-directory: ${{ env.WORKING_DIRECTORY }}

      - name: Upload package as artifact
        uses: actions/upload-artifact@v3
        with:
          name: 'app_${{ steps.bump.outputs.version }}'
          path: '${{ env.WORKING_DIRECTORY }}/app_${{ steps.bump.outputs.version }}.zip'
    
    outputs:
      version: ${{ steps.bump.outputs.version }}
      bump_commit_id: ${{ steps.auto-commit.outputs.commit_id }}
      artifact_name: 'app_${{ steps.bump.outputs.version }}'

  bump-version:
    uses: ./.github/workflows/bump-version.yaml
    with:
      context_name: frontend
      working_directory: ${{ env.WORKING_DIRECTORY }}
